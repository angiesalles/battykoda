run = "bash -c 'python3 init_db.py && python3 main.py'"
entrypoint = "main.py"

# Specifies Python version
[env]
PYTHON_VERSION = "3.10"

# Configures the language server for code intelligence
[language_server]
start = "pylsp"

# Configure the nix environment
[nix]
channel = "stable-22_11"